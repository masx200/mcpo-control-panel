// ================================================ //
// FILE: src/mcp_manager_ui/ui/static/js/main.js  //
// ================================================ //

// Пока оставим пустым.
// Сюда можно добавить JS для валидации форм,
// кастомной обработки ответов HTMX или других интерактивных элементов,
// если базового функционала HTMX будет недостаточно.

console.log("MCP Manager UI JS loaded.");

// Пример обработки события HTMX (если нужно)
// document.body.addEventListener('htmx:afterRequest', function(evt) {
//   if (evt.detail.successful) {
//     console.log("HTMX request successful:", evt.detail.xhr.responseURL);
//     // Можно добавить уведомления о успехе/ошибке
//   } else {
//     console.error("HTMX request failed:", evt.detail.xhr.status, evt.detail.xhr.statusText);
//   }
// });